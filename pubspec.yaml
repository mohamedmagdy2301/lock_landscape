name: lock_orientation_screen
description: "A Flutter package to lock the screen orientation to portrait mode and restore default orientations when disposed."
version: 0.0.1
repository: https://github.com/mohamedmagdy2301/lock_orientation_screen

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0


# The following section is specific to Flutter packages.
flutter:
